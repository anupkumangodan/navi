setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  buildpacks:
    - heroku/java
  config:
    MALLOC_ARENA_MAX: 1
build:
  docker:
    web: Dockerfile.web
    release: Dockerfile.release
  config:
    FOO: bar1

